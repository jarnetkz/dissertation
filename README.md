# Dispersion-2D3D

In the terminal at the root of the folder, run : 

source setup.rc

git config --global user.email "xxxx@gmail.com"
git config --global user.name xxxx

cd tutorialfenics
python3 poisson.py

